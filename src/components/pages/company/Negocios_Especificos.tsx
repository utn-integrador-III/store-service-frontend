import React from "react";

const Negocios_Especificos = () => {
  // Recieve the id from app component thorugh navigation
  // const { id } = useParams<{ id: string }>();

  return (
    <div>
      <h1>Testeo :p</h1>
    </div>
  );
};

export default Negocios_Especificos;
